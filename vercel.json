{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/stocksense run build",
  "outputDirectory": "artifacts/stocksense/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
